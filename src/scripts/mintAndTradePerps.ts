
async function mintAccountsAndTradePerps() {
    // create two fresh subaccounts
    // mint collateral for the subaccounts
    // have them trade perps against each other
    throw new Error("Not implemented");
}

mintAccountsAndTradePerps().then(console.log).catch(console.error);
